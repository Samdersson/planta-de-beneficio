# Usamos PHP con Apache
FROM php:8.1-apache

# Instalamos extensiones necesarias
RUN docker-php-ext-install mysqli pdo pdo_mysql

# Habilitamos mod_rewrite de Apache (importante si usas rutas amigables)
RUN a2enmod rewrite

# Copiamos el proyecto al directorio de Apache
COPY . /var/www/html/

# Ajustamos permisos
RUN chown -R www-data:www-data /var/www/html

# Exponemos el puerto
EXPOSE 80

# Iniciamos Apache en primer plano
CMD ["apache2-foreground"]
